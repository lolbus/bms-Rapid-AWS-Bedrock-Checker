[project]
name = "bedrock-checker"
version = "0.1.0"
description = "Portable Tkinter diagnostics for Claude-on-Bedrock spend (CloudWatch estimate, Cost Explorer billed, CE-calibrated)"
requires-python = ">=3.12,<3.13"
dependencies = ["boto3>=1.35"]

[dependency-groups]
dev = ["pyinstaller>=6.10"]
